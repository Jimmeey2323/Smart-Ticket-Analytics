# System Architecture & Visual Guide

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         SMART TICKET ANALYTICS                          │
│                    Departments, Teams & Routing System                   │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│                          CLIENT LAYER                                    │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐        │
│  │  Create Ticket  │  │ View Department │  │ Manage Teams    │        │
│  │   Components    │  │   Dashboard     │  │   & Members     │        │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘        │
│           │                    │                    │                   │
│           └────────────────────┼────────────────────┘                   │
│                                │                                        │
│                        API Calls & Hooks                               │
│                          /api/...                                       │
└───────────────┬────────────────────────────────────────────────────────┘
                │
┌───────────────┴────────────────────────────────────────────────────────┐
│                          BACKEND LAYER                                  │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │              REST API ENDPOINTS (routes-departments.ts)         │ │
│  ├──────────────────────────────────────────────────────────────┬──┤ │
│  │ GET /api/departments        │ Get /api/departments/:id/stats  │ │
│  │ POST /api/departments       │ PUT /api/departments/:id        │ │
│  │ GET /api/teams              │ POST /api/teams                 │ │
│  │ GET /api/team-members       │ POST /api/team-members          │ │
│  │ GET /api/routing-rules      │ POST /api/routing/find-target   │ │
│  │ POST /api/escalation/...    │                                 │ │
│  └─────────────────────────────┴──────────────────────────────────┘ │
│                                │                                      │
│                    ┌───────────┴────────────┐                        │
│                    │                        │                        │
│        ┌───────────▼──────────┐   ┌────────▼─────────────┐          │
│        │   Routing Service    │   │ Escalation Service   │          │
│        │                      │   │                      │          │
│        │ • Find assignment    │   │ • Check escalation   │          │
│        │ • Load balance       │   │ • Send notifications │          │
│        │ • Match skills       │   │ • Update ticket      │          │
│        └───────────┬──────────┘   └────────┬─────────────┘          │
│                    │                       │                        │
└────────────────────┼───────────────────────┼────────────────────────┘
                     │                       │
┌────────────────────┴───────────────────────┴────────────────────────┐
│                        DATABASE LAYER                               │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                    CORE TABLES                               │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │                                                              │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │ DEPARTMENTS (7 units)                                │  │ │
│  │  │ ├─ id (UUID)                                        │  │ │
│  │  │ ├─ name (Training, Sales, Marketing, etc.)          │  │ │
│  │  │ ├─ manager_id → users                               │  │ │
│  │  │ └─ code (TRN, SCS, MKT, etc.)                        │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                     ▼                                       │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │ TEAMS (per department)                               │  │ │
│  │  │ ├─ id (UUID)                                        │  │ │
│  │  │ ├─ name (Team A, Team B, etc.)                      │  │ │
│  │  │ ├─ department_id → departments                      │  │ │
│  │  │ ├─ manager_id → users                               │  │ │
│  │  │ ├─ max_capacity (10-20)                             │  │ │
│  │  │ ├─ current_load (real-time tracking)                │  │ │
│  │  │ └─ availability (available, busy, away)             │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                     ▼                                       │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │ TEAM MEMBERS (users in teams)                        │  │ │
│  │  │ ├─ user_id → users                                  │  │ │
│  │  │ ├─ team_id → teams                                  │  │ │
│  │  │ ├─ role_in_team (member, lead, backup_lead)        │  │ │
│  │  │ ├─ max_tickets (10 per member)                      │  │ │
│  │  │ ├─ current_ticket_count (real-time)                 │  │ │
│  │  │ ├─ skills (JSON array)                              │  │ │
│  │  │ └─ availability_status (available, busy, away)      │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                     ▼                                       │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │ ROUTING RULES (ticket assignment logic)             │  │ │
│  │  │ ├─ name & code (unique identifier)                  │  │ │
│  │  │ ├─ category_id → categories                         │  │ │
│  │  │ ├─ priority_level (low, medium, high, critical)     │  │ │
│  │  │ ├─ route_to_team_id → teams                         │  │ │
│  │  │ ├─ route_to_user_id → users                         │  │ │
│  │  │ ├─ load_balancing_strategy (round_robin, least,...)  │  │ │
│  │  │ ├─ required_skills (JSON array)                     │  │ │
│  │  │ ├─ auto_escalate_after_minutes (SLA time)           │  │ │
│  │  │ ├─ escalate_to_team_id (escalation target)          │  │ │
│  │  │ └─ is_active (enable/disable rules)                 │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                     ▼                                       │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │ ESCALATION RULES (auto-escalate logic)              │  │ │
│  │  │ ├─ priority_level                                   │  │ │
│  │  │ ├─ escalation_level (1, 2, 3, etc.)                 │  │ │
│  │  │ ├─ escalate_after_minutes (15, 30, 60, etc.)        │  │ │
│  │  │ ├─ escalate_to_role (team_member, manager, admin)   │  │ │
│  │  │ ├─ escalate_to_team_id (target team)                │  │ │
│  │  │ └─ notify_department_manager (boolean)              │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                                                              │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │         DATABASE FUNCTIONS (PostgreSQL Functions)            │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │ • get_best_assignment_target()                             │ │
│  │ • get_available_team_member()                              │ │
│  │ • escalate_ticket()                                        │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │  ROW LEVEL SECURITY (RLS Policies)                          │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │ • admin_* policies → Full access                           │ │
│  │ • manager_* policies → Department data only                │ │
│  │ • team_* policies → Team data only                         │ │
│  │ • user_* policies → Own data only                          │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

### Ticket Creation & Routing Flow

```
┌────────────────┐
│  User Creates  │
│    Ticket      │
└────────┬───────┘
         │
         ▼
┌─────────────────────────────┐
│  Extract Ticket Details:    │
│  • category_id              │
│  • priority                 │
│  • department               │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│  Call Routing Service                       │
│  get_best_assignment_target(                │
│    category_id,                             │
│    priority,                                │
│    departmentName                           │
│  )                                          │
└────────┬────────────────────────────────────┘
         │
         ▼
┌───────────────────────────────────────────────────┐
│  Find Matching Routing Rule                       │
│  SELECT * FROM routing_rules                      │
│  WHERE is_active = true                           │
│  AND category_id = ?                              │
│  AND (priority_level = ? OR priority_level NULL)  │
│  ORDER BY priority ASC                            │
└────────┬───────────────────────────────────────────┘
         │
    ┌────┴────┐
    │          │
    ▼          ▼
┌─────────┐ ┌──────────────┐
│ Found   │ │ Not Found    │
│ Rule    │ │ Use Default  │
└────┬────┘ └──────┬───────┘
     │             │
     ▼             ▼
┌──────────────────────────────────────────────┐
│  Determine Assignment Target:                │
│  • Team ID or User ID from rule              │
│  • OR find team in department                │
└────┬───────────────────────────────────────┬─┘
     │                                       │
     ▼                                       ▼
┌──────────────────────────┐   ┌─────────────────────────┐
│ Select Team Members      │   │ Get Available User      │
│ by Load Balancing        │   │                         │
│ Strategy:                │   │ User with fewest        │
│ • Round Robin            │   │ current tickets &       │
│ • Least Loaded (Default) │   │ status = 'available'    │
│ • Skill Based            │   └────────┬────────────────┘
│ • Random                 │            │
└────────┬─────────────────┘            │
         │                              │
         └──────────────┬───────────────┘
                        │
                        ▼
         ┌──────────────────────────┐
         │  Create Ticket with:     │
         │  • assignee_id = user_id │
         │  • department = dept_name│
         │  • status = 'open'       │
         └──────────┬───────────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │  Update Team Loads:      │
         │  • team.current_load++   │
         │  • member.tickets++      │
         └──────────┬───────────────┘
                    │
                    ▼
         ┌──────────────────────────┐
         │  Return Assigned Ticket  │
         │  to User                 │
         └──────────────────────────┘
```

---

## 🔄 Escalation Flow

```
┌──────────────────────────────┐
│  Ticket Created &            │
│  Assigned to Team Member     │
└────────────┬─────────────────┘
             │
             ▼
    ┌────────────────────┐
    │ Start Timer        │
    │ based on Priority  │
    │ and SLA Rules      │
    └────────┬───────────┘
             │
      ┌──────▼──────┐
      │ Time Elapsed│
      └──────┬──────┘
             │
    ┌────────▼────────────────────┐
    │ Check Escalation Trigger:   │
    │ • SLA time exceeded?        │
    │ • Manual escalation?        │
    │ • Business rule trigger?    │
    └────────┬────────────────────┘
             │
      ┌──────┴──────┐
      │             │
      ▼             ▼
   YES            NO
   │               │
   │               └─► Continue normal flow
   │
   ▼
┌─────────────────────────────┐
│ Load Escalation Rule        │
│ by Priority Level           │
│ (Critical → High → Medium)  │
└────────┬────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Determine Escalation Target:     │
│ • Next level team                │
│ • Manager/Supervisor             │
│ • Department head                │
│ • Executive (if critical)        │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Update Ticket:               │
│ • is_escalated = true        │
│ • escalated_at = NOW()       │
│ • escalated_to_id = user_id  │
│ • escalation_reason = reason │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Send Notifications:          │
│ • To escalated-to user       │
│ • To department manager      │
│ • To original assignee       │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Create Ticket History Entry  │
│ for Audit Trail              │
└──────────────────────────────┘
```

---

## 🏭 Load Balancing Strategies

### 1. Round Robin
```
Queue: [User A, User B, User C, User D]
Ticket 1 → User A (round++)
Ticket 2 → User B (round++)
Ticket 3 → User C (round++)
Ticket 4 → User D (round++)
Ticket 5 → User A (wrap around)
```

### 2. Least Loaded (Default)
```
Current Load:
User A: 3 tickets
User B: 5 tickets (< current max)
User C: 2 tickets ← LEAST LOADED
User D: 4 tickets

Ticket → Assign to User C
```

### 3. Skill Based
```
Ticket Requirements: [billing_support, vip_care]

User A Skills: [general, vip_care]       ✗ Missing billing
User B Skills: [billing, general]        ✗ Missing vip_care
User C Skills: [billing, vip_care]       ✓ ALL SKILLS MATCH
User D Skills: [training]                ✗ No matching skills

Ticket → Assign to User C
```

### 4. Random
```
Random Selection:
Team Members: [User A, User B, User C, User D]
Pick random: User B
Ticket → Assign to User B
```

---

## 👥 Role-Based Access Control (RBAC)

```
┌────────────────────────────────────────────────────────────────┐
│                         ROLE HIERARCHY                         │
└────────────────────────────────────────────────────────────────┘

                    ▲
                    │
                    │ Escalation Path
                    │
        ┌───────────┴────────────┐
        │                        │
    ┌─────────────┐        ┌──────────┐
    │   ADMIN     │        │ MANAGER  │
    │ Full Access │◄───────┤ Dept Ops │
    │             │        │          │
    └─────┬───────┘        └──────┬───┘
          │                       │
          │  supervises           │ supervises
          │                       │
    ┌─────▼───────────────────────▼─────┐
    │      TEAM LEAD / SUPERVISOR       │
    │  • View team members              │
    │  • Assign tickets                 │
    │  • Manage team workload           │
    └─────┬─────────────────────────────┘
          │
          │  oversees
          │
    ┌─────▼───────────────────────────┐
    │       TEAM MEMBERS              │
    │  • View own tickets             │
    │  • Update ticket status         │
    │  • Add comments                 │
    └─────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    RLS POLICY MATRIX                            │
├────────────────────┬──────┬─────────┬──────────┬─────────────┤
│ Resource           │Admin │Manager  │ Lead     │ Member      │
├────────────────────┼──────┼─────────┼──────────┼─────────────┤
│ All Departments    │  ✓   │  ✗      │  ✗       │  ✗          │
│ Own Department     │  ✓   │  ✓      │  ✓       │  ✓          │
│ All Teams          │  ✓   │  ✗      │  ✗       │  ✗          │
│ Dept Teams         │  ✓   │  ✓      │  ✓       │  ✓          │
│ Own Team           │  ✓   │  ✓      │  ✓       │  ✓          │
│ All Team Members   │  ✓   │  ✗      │  ✗       │  ✗          │
│ Dept Team Members  │  ✓   │  ✓      │  ✓       │  ✓          │
│ Own Team Members   │  ✓   │  ✓      │  ✓       │  ✓          │
│ All Tickets        │  ✓   │  ✗      │  ✗       │  ✗          │
│ Dept Tickets       │  ✓   │  ✓      │  ✓       │  ✓          │
│ Assigned Tickets   │  ✓   │  ✓      │  ✓       │  ✓          │
│ Own Profile        │  ✓   │  ✓      │  ✓       │  ✓          │
└────────────────────┴──────┴─────────┴──────────┴─────────────┘
```

---

## 📈 Department Structure Example

```
PHYSIQUE 57 ORGANIZATION
│
├─ TRAINING (TRN)
│  ├─ Manager: [Training Lead]
│  ├─ Teams:
│  │  ├─ Training Team A (10 capacity)
│  │  │  ├─ Vivaran Dhasmana (lead, 8 tickets)
│  │  │  ├─ Anisha Shah (member, 5 tickets)
│  │  │  └─ [Team members...]
│  │  └─ Training Team B (8 capacity)
│  │
│  └─ Routing Rules:
│     └─ "Training" category → Training Team A (least_loaded)
│
├─ SALES & CLIENT SERVICING (SCS)
│  ├─ Manager: Jimmeey (Admin)
│  ├─ Teams:
│  │  ├─ Sales Team A (10 capacity)
│  │  │  ├─ [Team members...]
│  │  └─ Sales Team B (8 capacity)
│  │
│  └─ Routing Rules:
│     ├─ "Billing" category → Sales Team A (least_loaded)
│     └─ "Complaint" category → Sales Team A (escalate after 15 min)
│
├─ MARKETING (MKT)
│  ├─ Manager: Ayesha Munot
│  ├─ Teams:
│  │  └─ Marketing Team A (10 capacity)
│  │
│  └─ Routing Rules:
│     └─ "Marketing" category → Marketing Team A
│
└─ [Other Departments...]
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│     APPLICATION LAYER SECURITY          │
├─────────────────────────────────────────┤
│  • JWT Token Authentication              │
│  • Role-based Authorization              │
│  • Input Validation                      │
└────────────────────┬────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────┐
│    ROW LEVEL SECURITY (RLS)             │
├─────────────────────────────────────────┤
│  • Database-level enforcement            │
│  • Cannot bypass with direct SQL         │
│  • User role determines visible rows     │
│  • Policies on all sensitive tables      │
└────────────────────┬────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────┐
│      DATABASE LAYER SECURITY            │
├─────────────────────────────────────────┤
│  • SSL Connection (Supabase)             │
│  • Connection Pooling                    │
│  • Encrypted Passwords                   │
│  • Audit Trail (ticket_history)          │
└────────────────────┬────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────┐
│     INFRASTRUCTURE SECURITY             │
├─────────────────────────────────────────┤
│  • VPN/Firewall                          │
│  • DDoS Protection                       │
│  • Backup & Recovery                     │
│  • Monitoring & Alerts                   │
└─────────────────────────────────────────┘
```

---

## 📊 Performance Optimizations

```
INDEXES CREATED:
├─ departments
│  ├─ idx_departments_manager_id
│  └─ idx_departments_parent_id
│
├─ teams
│  ├─ idx_teams_department_id
│  └─ idx_teams_manager_id
│
├─ team_members
│  ├─ idx_team_members_user_id
│  ├─ idx_team_members_team_id
│  ├─ idx_team_members_department_id
│  └─ idx_team_members_availability
│
├─ routing_rules
│  ├─ idx_routing_rules_category
│  ├─ idx_routing_rules_department
│  ├─ idx_routing_rules_team
│  ├─ idx_routing_rules_priority
│  └─ idx_routing_rules_active
│
└─ tickets
   ├─ idx_ticket_status
   ├─ idx_ticket_priority
   ├─ idx_ticket_assignee
   ├─ idx_ticket_category
   ├─ idx_ticket_department
   └─ idx_ticket_sla
```

---

## 🧮 Complexity Analysis

| Operation | Time | Query |
|-----------|------|-------|
| Get departments | O(1) | Direct SELECT with index |
| Find routing rule | O(log n) | B-tree index on priority |
| Get team members | O(m) | m = team size |
| Get available user | O(m) | m = team size, worst case |
| Escalate ticket | O(log n) | B-tree index lookup |
| Update loads | O(1) | Direct UPDATE with index |

**n** = number of records
**m** = number of team members (typically 5-20)

---

## ✨ This documentation provides a complete visual guide to the system architecture, data flow, and security implementation.
